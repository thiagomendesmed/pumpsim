export async function openCustomerCenter(): Promise<void> {
  // Web usa Stripe Customer Portal (a ser implementado).
  // Quando a action Convex `createStripePortalSession` existir, aqui redireciona pra URL retornada.
  console.info("Customer center on web: Stripe portal not yet implemented");
}
